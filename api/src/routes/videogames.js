const {Router} = require ('express');
const axios = require ('axios');
const {API_KEY, Videogame, Genre, conn} = require ('../db');
const router = express.Router ();

router.get ('/', async (req, res) => {
   
})
























module.exports = router;
